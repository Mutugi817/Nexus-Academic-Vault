services:
  - type: web
    name: exam-scraper-service
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node scraper.js
    envVars:
      - key: NODE_ENV
        value: production